# dotfiles

## How To Use

```shell
git clone --recursive git@github.com:m02uku/.dotfiles.git ~/.dotfiles
cd ~/.dotfiles
make run {project}
# if want to run in current directory:
make run $PWD
```

## Requirements

- Docker
